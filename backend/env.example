# LAMBA - Learning Activities & Machine-Based Assessment
# Environment configuration file
# Copy this to .env and fill in your values

# LTI Configuration (REQUIRED)
# These credentials must match your LTI tool configuration in Moodle
LTI_SECRET=my_lti_secret
OAUTH_CONSUMER_KEY=lamba_key

# Database Configuration (OPTIONAL)
# Default: sqlite:///./lamba.db
DATABASE_URL=sqlite:///./lamba.db

# HTTPS Configuration for production (OPTIONAL)
# Set to true when deploying with HTTPS
HTTPS_ENABLED=false

# CORS Origins (OPTIONAL)
# Comma-separated list of allowed origins for production
# Use * for development, specific domains for production
ALLOWED_ORIGINS=*

# LAMB Configuration (OPTIONAL)
# Bearer token for LAMB API authentication
LAMB_BEARER_TOKEN=XXXX
LAMB_API_URL=XXXX
LAMB_TIMEOUT=30

ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# Debug mode - when true, shows raw AI responses for instructors
# Set to true to enable debug information for AI evaluation
DEBUG=false

# Default activity language (en, es, ca, eu)
# Used when teacher creates an activity without specifying a language
# The activity language determines: group display names (GRUPO_1 vs GROUP_1),
# and forces the UI language for all users viewing the activity
DEFAULT_ACTIVITY_LANGUAGE=en